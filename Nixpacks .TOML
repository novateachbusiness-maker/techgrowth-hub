[phases.setup]
nixPkgs = [
  "chromium",
  "chromedriver",
  "python311",
]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium",
  "python -m playwright install-deps chromium",
]

[start]
cmd = "python techgrowth_hub.py"
